import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Define middleware to handle internationalization
  // The middleware will redirect users based on their locale preference
};

export default nextConfig;
